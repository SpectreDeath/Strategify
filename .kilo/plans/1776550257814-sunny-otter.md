# Strategify Implementation Plan - Phases 19 & 20 (Priority)

## Assessment Summary

| Phase | Description | Current Status |
|-------|-------------|----------------|
| 19 | FastAPI + React Integration | **PARTIALLY DONE** - FastAPI exists at `strategify/web/api.py`, React at `frontend/` |
| 20 | XAI (Explainable AI) | **NOT STARTED** - No UI for Prolog belief graph or MCTS branches |

**Focus**: Complete Phase 19 integration, then implement Phase 20 XAI visualization.

---

## Implementation Plan

### Phase 19: Complete FastAPI + Frontend Integration

**Goal**: Verify frontend fully communicates with backend API

#### Tasks:
1. **Audit existing API endpoints** - Test each endpoint in `strategify/web/api.py`:
   - `GET /api/status`
   - `POST /api/simulation/start`
   - `POST /api/simulation/stop`
   - `POST /api/simulation/step`
   - `GET /api/simulation/state`

2. **Add missing endpoints** for Phase 20:
   - `GET /api/agents/{id}/beliefs` - Fetch Prolog belief facts
   - `GET /api/agents/{id}/mcts-branches` - Fetch Clojure MCTS timelines

3. **Verify frontend integration**:
   - Check `frontend/src/services/api.js` or equivalent
   - Ensure CORS handles production builds

4. **Add API docs** - OpenAPI/Swagger at `/docs`

---

### Phase 20: XAI Visualization

**Goal**: Visualize agent "minds" - Prolog beliefs + MCTS branches

#### Tasks:

1. **Prolog Belief Graph Visualizer**:
   - Create API endpoint to expose belief facts from `strategify/logic/traits.pl`
   - Build React component to render beliefs as interactive graph
   - Use D3.js or react-force-graph

2. **MCTS Branch Browser**:
   - Create API endpoint for Clojure timeline data
   - Build React component to display counterfactual branches
   - Show probability weights per branch

3. **Integration**:
   - Add to existing frontend routes (`/analysis` or new `/xai` route)
   - Connect to agent selection UI

---

## Immediate Action Items

1. [ ] Run backend: `python -m strategify.web.server`
2. [ ] Test endpoints with curl or Postman
3. [ ] Check frontend API service configuration
4. [ ] Add belief/MCTS endpoints to `strategify/web/api.py`
5. [ ] Create `frontend/src/components/BeliefGraph.jsx`
6. [ ] Create `frontend/src/components/MCTSBrowser.jsx`
7. [ ] Test full integration